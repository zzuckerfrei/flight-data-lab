# 개인 작업 노트·설계 초안·자격증명은 추적하지 않는다 (repo엔 완성 산출물만).
private/

# 시크릿 — GCP 서비스계정 키, OpenSky 자격증명, 환경변수 (절대 커밋 금지)
*.json.key
*-sa.json
service-account*.json
.env
.env.*
secrets/
credentials/

# Python
.venv/
__pycache__/
*.pyc

# dbt 산출물
dbt/target/
dbt/dbt_packages/
dbt/logs/
.user.yml

# Airflow 로컬 산출물
logs/
airflow.db
airflow.cfg
standalone_admin_password.txt

# k8s/helm 임시
*.tmp

# 데이터 (재생성 가능 — 누적 스냅샷은 GCS/BigQuery에 있음)
data/
*.parquet

# 로컬 개발 도구 상태 (OMC 오케스트레이션 등)
.omc/

# macOS
.DS_Store
