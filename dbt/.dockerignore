# 이미지에 넣지 않을 것 (가볍게 + 비밀/캐시 제외)
.venv/
target/
dbt_packages/
logs/
Dockerfile
.dockerignore

# 시크릿·불필요 방어 (실수로라도 이미지에 안 들어가게)
.git/
.gitignore
*.json          # SA 키 등 자격증명 파일 방지 (dbt/ 아래엔 없지만 방어적)
*.log
